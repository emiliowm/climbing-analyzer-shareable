## Frontend Workspace

This directory contains the Next.js review application for Climbing Analyzer.

Use the root [README.md](../README.md) for the project overview, local setup, and operational notes. The frontend should be started with:

```bash
npm install
npm run dev -- --hostname 127.0.0.1 --port 3000
```
